\RequirePackage{lmodern}
\RequirePackage[utf8]{inputenc}
\RequirePackage[T1]{fontenc}
\RequirePackage{amsmath}
\RequirePackage{amsfonts}
\RequirePackage{amssymb}
\RequirePackage{graphicx}
\RequirePackage[french]{babel}
\RequirePackage[notitlepage,fancysections,pagenumber]{sauveterre}

\RequirePackage{ifthen}

\RequirePackage{enumitem}

\RequirePackage{indentfirst}
\RequirePackage{lipsum}

\RequirePackage{cases}
\RequirePackage{multicol}

%formatting date
\RequirePackage[ddmmyyyy]{datetime}
\renewcommand{\dateseparator}{/}

%\RequirePackage{geometry}
%\geometry{
% a4paper,
% total={170mm,257mm},
% left=20mm,
% top=20mm,
% }

\RequirePackage{pgf,tikz}
\RequirePackage{mathrsfs}
\usetikzlibrary{arrows}

\RequirePackage[export]{adjustbox}

\newcommand{\mainHeader}{
   \renewcommand{\notitlepageupperline}{
      \noindent%
      \begin{minipage}{\textwidth}
      \centering
      \begin{minipage}{0.33\textwidth}
      \raisebox{-0.5\height}
      {\color{bleu-sauveterre}%
      \includegraphics[height=0.8\hauteurlogovert]
      {\polylogovert}
      }
      \end{minipage}%
      \begin{minipage}{0.33\textwidth}
      \color{bleu-sauveterre}%
      \centering\polydatesave
      \polyauthorsave
      \hspace{3em}
      \end{minipage}%
      \begin{minipage}{0.33\textwidth}
      \color{bleu-sauveterre}%
      \flushright\raisebox{-0.5\height}
      {\includegraphics[height=0.8\hauteurlogovert]{logo-federal}
      }

      {\color{black}\No \hspace{3em}/CES/DIR}
      \end{minipage}
      \end{minipage}
   }
}

\newcommand{\annexHeader}{
   \renewcommand{\notitlepageupperline}{
      \noindent%
      \begin{minipage}{\textwidth}
      \centering
      \begin{minipage}{0.33\textwidth}
      \raisebox{-0.5\height}
      {\color{bleu-sauveterre}%
      \includegraphics[height=0.8\hauteurlogovert]
      {\polylogovert}
      }
      \end{minipage}%
      \begin{minipage}{0.33\textwidth}
      \color{bleu-sauveterre}%
      %\centering\polydatesave
      %\polyauthorsave
      \hspace{3em}
      \end{minipage}%
      \begin{minipage}{0.33\textwidth}
      \color{bleu-sauveterre}%
      \flushright\raisebox{-0.5\height}
      {\includegraphics[height=0.8\hauteurlogovert]{logo-federal}
      }

      \polydatesave
      \end{minipage}
      \end{minipage}
   }
}

%enumerate environment formatting
\renewcommand{\labelenumi}{\textbf{\arabic{enumi}.}}
\renewcommand{\labelenumii}{\textbf{\arabic{enumi}.\arabic{enumii}.}}
\renewcommand{\labelenumiii}{\textbf{\arabic{enumi}.\arabic{enumii}.\arabic{enumiii}.}}

%formatting titles
\RequirePackage{titlesec}
%\titleformat*{\section}{\centering\Large\bfseries}
%\titleformat*{\subsection}{\centering\large\bfseries}

\titleformat*{\section}{\centering\Large\bfseries}
%\titleformat*{\subsection}{\centering\large\bfseries}{Article~\thesubsection :}
\titleformat{\subsection}{\centering\large\bfseries}{Article~\thesubsection~:}{.5em}{}[\vspace*{-0.3\baselineskip}%
\includegraphics{\polyfiletcourtbleuclair}]

%change sec and subsec numbering
\renewcommand{\thesection}{Section \Roman{section}.}
\renewcommand{\thesubsection}{\arabic{subsection}}


\renewcommand{\labelenumi}{\arabic{enumi}.}


\renewcommand*\contentsname{Plan}

\renewcommand{\emph}[1]{\textit{\textbf{#1}}}


\RequirePackage{framed}

%\setlength{\parindent}{0pt}%remove indentation

%\RequirePackage{fancyhdr}
%\RequirePackage{lastpage}
%\pagestyle{fancy}
%\fancyhf{}
%%headers and footers
%\lhead{Centre Équestre de Sauveterre}
%\chead{Règlement intérieur}
%\rhead{Version du \today}
%\rfoot{Page \thepage \ sur \pageref{LastPage}}

%\fancypagestyle{firstpage}{%
%  \fancyhf{}
%  \renewcommand\headrulewidth{0pt}
%  \fancyfoot[R]{Page \thepage \ sur \pageref{LastPage}}
%}
%
%\title{\vspace{-50pt}
%\includegraphics[width=0.4\textwidth , left]{logo2-v2.eps}~
%\\[8pt]
%{\huge \textsc{Règlement intérieur}}
%}
